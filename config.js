// Kamal Jewellers — site config
// The Worker API lives at the same origin on the live site (/api/*).
// On localhost this file is a no-op; the live site reads from the Worker.
window.KAMAL_CONFIG = {
  API_BASE: '/api',
};
