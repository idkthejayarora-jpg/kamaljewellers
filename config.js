/* ============================================================
   KAMAL JEWELLERS — site configuration
   ------------------------------------------------------------
   Fill these in AFTER you create your free Supabase project.
   See SETUP.md for step-by-step instructions.

   Until you fill these in, the site runs in "demo mode":
   it shows the default content and the enquiry form pretends
   to send (nothing is stored). That's fine for previewing.

   These two values are SAFE to be public (they live in the
   browser by design). Real security is enforced by Supabase
   Row-Level Security — never put the service_role key here.
   ============================================================ */
window.KAMAL_CONFIG = {
  SUPABASE_URL:      "YOUR_SUPABASE_URL",       // e.g. https://abcdxyz.supabase.co
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY",  // the "anon public" key
  ADMIN_EMAIL:       "admin@kamaljewellers.in"  // the single admin login (set in SETUP.md step 5)
};
